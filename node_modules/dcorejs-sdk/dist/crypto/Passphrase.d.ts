/// <reference types="node" />
export declare class Passphrase {
    static generate(count?: number): Passphrase;
    private static WORD_COUNT;
    readonly words: string[];
    constructor(entropy: Buffer, seed: string[], count: number);
    toString(): string;
    private create;
}
