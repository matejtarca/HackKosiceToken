/// <reference types="node" />
export declare class Wif {
    readonly privateKey: Buffer;
    private static encode;
    readonly encoded: string;
    constructor(privateKey: Buffer, version: number);
}
