/// <reference types="node" />
import * as Long from "long";
import { Address } from "./Address";
import { Passphrase } from "./Passphrase";
import { Wif } from "./Wif";
export declare class ECKeyPair {
    static parseWif(base58: string): ECKeyPair;
    static generate(): ECKeyPair;
    static generateFromPhrase(phrase: Passphrase | string, sequence?: number): ECKeyPair;
    private static VERSION;
    private static COMPRESSED;
    private static COMPACT;
    readonly privateKey: Buffer;
    readonly publicKey: Buffer;
    /**
     * the public key is always in a compressed format in DCore
     */
    private constructor();
    readonly publicAddress: Address;
    readonly privateWif: Wif;
    sign(data: Buffer): string | undefined;
    secret(recipient: Address, nonce: Long): Buffer;
}
