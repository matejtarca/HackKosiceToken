import { FullAccount } from "../../../models/FullAccount";
import { BaseRequest } from "./BaseRequest";
export declare class GetFullAccounts extends BaseRequest<Map<string, FullAccount>> {
    constructor(nameOrId: string[], subscribe: boolean);
}
