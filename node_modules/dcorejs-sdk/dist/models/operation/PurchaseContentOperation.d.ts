import { Credentials } from "../../crypto/Credentials";
import { AssetAmount } from "../AssetAmount";
import { ChainObject } from "../ChainObject";
import { Content } from "../Content";
import { PubKey } from "../PubKey";
import { Regions } from "../Regions";
import { BaseOperation } from "./BaseOperation";
export declare class PurchaseContentOperation extends BaseOperation {
    static create(credentials: Credentials, content: Content): PurchaseContentOperation;
    uri: string;
    consumer: ChainObject;
    price: AssetAmount;
    publicElGamal: PubKey;
    regionCode: number;
    /**
     * Request to purchase content operation constructor
     *
     * @param uri uri of the content
     * @param consumer chain object id of the buyer's account
     * @param price price of content, can be equal to or higher then specified in content
     * @param publicElGamal public el gamal key
     * @param regionCode region code of the consumer
     */
    constructor(uri: string, consumer: ChainObject, price: AssetAmount, publicElGamal: PubKey, regionCode?: Regions);
}
