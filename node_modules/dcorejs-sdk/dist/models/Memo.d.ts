import * as Long from "long";
import { Address } from "../crypto/Address";
import { ECKeyPair } from "../crypto/ECKeyPair";
export declare class Memo {
    static createPublic(message: string): Memo;
    static createEncrypted(message: string, keyPair: ECKeyPair, recipient: Address, nonce?: Long): Memo;
    from?: Address;
    to?: Address;
    message: string;
    nonce: Long;
    private constructor();
    decrypt(keyPair: ECKeyPair): string;
    private decryptWithChecksum;
}
